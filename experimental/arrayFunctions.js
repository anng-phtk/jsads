/**
 * @param {number[]} nums
 * @return {number}
 */
var maxProduct = function(nums) {
    let maxNum = Math.max(...nums);
    let newArray = nums.filter((maxNum, idx) => {
        
    });
    //let nextMaxNum = Math.max(...)

    console.log(newArray)
};

maxProduct([4,3,2,1, 5]);
